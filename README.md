# horizon-film-lab
A macOS app for inverting scanned colour negatives and printing them the way a minilab would. Feed it raw captures from whatever you're scanning with, and it does the density inversion, film-base subtraction, exposure, tone, and colour balance, then sends the result through either a built-in RA-4-style print curve, a loaded ICC profile or LUT.
